import { CreateCustomerRequest } from './create-customer-request.model';

describe('CreateCustomerRequest', () => {
  it('should create an instance', () => {
    expect(new CreateCustomerRequest()).toBeTruthy();
  });
});
